// export function attachToken(req, res, next) {
//     if (req.user) {
//       const newToken = generateToken({ id: req.user.id, email: req.user.email });
//       res.locals.token = "Bearer " + newToken; // attach token to response
//     }
//     next();
//   }
  